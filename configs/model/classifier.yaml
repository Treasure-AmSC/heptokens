---
optimizer:
  _target_: qhep.models.optimisers.AdamWS
  _partial_: true
  lr: 5.0e-4
  betas: [0.9, 0.95]
  weight_decay: 0.01
scheduler:
  _target_: qhep.models.utils.linear_warmup_cosine_decay
  _partial_: true
  warmup_steps: 1_000
  total_steps: -1  # Will automatically sync to length of dataloader * max_epochs
