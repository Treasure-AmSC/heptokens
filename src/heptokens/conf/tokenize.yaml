---
defaults:
  - hydra: default
  - datamodule: ???  # must be supplied by the user
  - _self_
ckpt_path: ???
output_dir: ???
datamodule:
  train_frac: 0.0
  val_frac: 0.0
  test_frac: 1.0
  seed:
  batch_size: 2048
  chunk_size: 50000
trainer:
  _target_: lightning.Trainer
  accelerator: auto
  devices: 1
  enable_progress_bar: true
full_path: ${output_dir}
